# ARTraining

Unity AR training project.

## Project Structure

- `Assets/` - Unity scenes, scripts, prefabs, training content, and documentation.
- `Packages/` - Unity package manifest and lock file.
- `ProjectSettings/` - Unity project settings.

Generated Unity folders such as `Library/`, `Temp/`, `Logs/`, and local build outputs are intentionally ignored.
